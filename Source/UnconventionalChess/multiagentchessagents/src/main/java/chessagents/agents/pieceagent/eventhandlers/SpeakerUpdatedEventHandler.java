package chessagents.agents.pieceagent.eventhandlers;

import chessagents.agents.pieceagent.events.SpeakerUpdatedEvent;
import chessagents.agents.pieceagent.pieces.PieceAgent;

public class SpeakerUpdatedEventHandler extends PieceEventHandler<SpeakerUpdatedEvent> {

    public void apply(SpeakerUpdatedEvent event, PieceAgent agent) {
        // do nothing
    }
}
